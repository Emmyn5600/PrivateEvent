<%= form_with(model: create) do |form| %>
  <% if create.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(create.errors.count, "error") %> prohibited this create from being saved:</h2>

      <ul>
        <% create.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Event %>
    <%= form.text_field :Event %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :location %>
    <%= form.text_field :location %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
